<%= form_for(@salary_schedule,:html => { style: "text-align: left;"} ) do |f| %>
  <% if @salary_schedule.errors.any? %>
    <div id="error_explanation">
      <div id="notice" class="alert alert-warning alert-dismissable">
            <i class="fa fa-exclamation"></i>
            <button class="close" aria-hidden="true" data-dismiss="alert" type="button">×</button>
            <p ><%= notice %></p>
      <h3><%= pluralize(@salary_schedule.errors.count, "error") %> prohibited this salary_schedule from being saved:</h3>
      <ul>
      <% @salary_schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
      </div>
    </div>
  <% end %>

  <div class="form-group" style="width: 50%">
  <label>Date:</label>
  <div class="input-group">
    <div class="input-group-addon">
      <i class="fa fa-calendar"></i>
    </div>
    <%= f.text_field :date, class: "form-control date_custom", :"data-date-format" => "yyyy-mm-dd"  %>
  </div>
</div>

  <div class="actions">
    <%= f.submit btn_title, :class=>'btn btn-default' %>
  </div>
<% end %>
