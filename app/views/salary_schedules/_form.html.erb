<%= form_for(@salary_schedule,:html => { style: "text-align: left;"} ) do |f| %>
  <% if @salary_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salary_schedule.errors.count, "error") %> prohibited this salary_schedule from being saved:</h2>

      <ul>
      <% @salary_schedule.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group" style="width: 50%">
  <label>Tanggal:</label>
  <div class="input-group">
    <div class="input-group-addon">
      <i class="fa fa-calendar"></i>
    </div>
    <%= f.text_field :date, :placeholder=> "Tanggal" , class: "form-control pull-right date_custom", required: true  %>
  </div>
</div>

  <div class="actions">
    <%= f.submit btn_title, :class=>'btn btn-default' %>
  </div>
<% end %>
