<%= form_for(@allowance_category,:html => { style: "text-align: left;"}) do |f| %>
<% if @allowance_category.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(@allowance_category.errors.count, "error") %> prohibited this allowance_category from being saved:</h2>

  <ul>
    <% @allowance_category.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
    <% end %>
  </ul>
</div>
<% end %>





<div class="form-group" style="width: 50%">
  <label>Nama Tunjangan:</label>
  <%= f.text_field :name , :placeholder=> 'Nama Kategori Tunjangan', :class=> 'form-control'%>
</div>
<div class="wrapeer-sub-category" id="nested-sub-category">
  <%= f.fields_for :allowance_sub_categories do |builder| %>
  <%= render "allowance_sub_category_fields", :f => builder %>
  <% end %>
</div>
<p>
  <%= link_to_function "Tambah", "add_fields2()" %>
</p>
<%= f.submit nil, :class=>'btn btn-default' %>
<% end %>
