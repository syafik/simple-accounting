<%= form_for(@allowance_category) do |f| %>
  <% if @allowance_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@allowance_category.errors.count, "error") %> prohibited this allowance_category from being saved:</h2>

      <ul>
      <% @allowance_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <table>
      <tr>
        <td>
          <%= f.label :name %>
        </td>
        <td>
          <%= f.text_field :name , :placeholder=> 'Nama Tunjangan', :class=> 'form-control'%>
        </td>
      </tr>
    </table>
    <%= f.submit nil, :class=>'btn btn-primary' %>
  </div>
<% end %>
