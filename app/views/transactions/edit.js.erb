$("#containerModal").html("<%= escape_javascript(render :partial => "/transactions/edit") %>");
$('#transaction_date').datepicker({ format: 'yyyy-mm-dd' });
$('#editTransactionModal').modal('show');
$('#transaction_value').maskMoney({precision: 0});
