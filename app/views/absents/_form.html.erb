<%= form_for(@absent) do |f| %>
  <% if @absent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@absent.errors.count, "error") %> prohibited this absent from being saved:</h2>

      <ul>
      <% @absent.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
<table>
    <tr>
     <td><%= f.label :date %></td>
     <td><%= f.date_select :date %></td>
    </tr>
    <tr>
     <td><%= f.label :user_id %></td>
     <td><%= f.number_field :user_id, :value => current_user.id %></td>
    </tr>
    <tr>  
     <td><%= f.label :categories %></td>
     <td><%= f.select :categories, options_for_select([['Masuk',1], ['Alfa',2], ['Izin',3], ['Sakit',4]])%></td>
    </tr>
    <tr>  
     <td><%= f.label :description %></td>
     <td><%= f.text_area :description, :cols => "30", :rows => "10" %></td>
    </tr> 
    <tr>
     <td><%= f.submit nil, :class=>'btn btn-default' %></td>
    </tr>
  </div>
</table>
<% end %>
